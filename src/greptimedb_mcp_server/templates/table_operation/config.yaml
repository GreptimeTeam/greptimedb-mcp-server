description: "Table diagnostics: schema, region health, storage, and cluster metadata for GreptimeDB."
version: "1.0"
arguments:
  - name: "table"
    description: "The table to analyze (required). Use schema.table format if needed."
    required: true
metadata:
  tags:
    - diagnostics
    - schema
    - region
    - storage
  suggested_use: "Inspect table structure, region distribution, storage usage, and cluster metadata."
  output_format: "markdown"
